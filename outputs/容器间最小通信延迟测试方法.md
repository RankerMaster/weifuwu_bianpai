# 容器间最小通信延迟测试方法

## 1. 指标说明

| 指标名称 | 指标含义 | 测试对象 |
|---|---|---|
| 容器间最小通信延迟 | 两个容器之间数据传输可达到的最小通信时延 | 高实时高可靠虚拟控制器 |

本测试使用 ICMP Ping 方法测量两个容器之间的通信时延。Ping 输出的 `time` 表示一次请求和响应的往返时延，即 RTT。

由于指标描述为“两容器间数据传输的单向时延”，因此在使用 Ping 测试时，采用如下近似计算：

```text
最小单向通信时延 ≈ 最小往返时延 RTT_min / 2
```

## 2. 测试环境

测试环境建议记录如下信息：

| 项目 | 内容 |
|---|---|
| 宿主机型号 | 例如 PowerEdge R750xs |
| 操作系统 | 例如 Ubuntu / Debian / CentOS |
| Docker 版本 | 使用 `docker --version` 查看 |
| 容器网络 | Docker bridge 自定义网络 |
| 发送端容器 | latency-c1 |
| 接收端容器 | latency-c2 |
| 测试次数 | 1000 次 |

## 3. 测试步骤

### 3.0 下载alpine镜像

```bash
docker pull alpine:latest
```

### 3.1 创建 Docker 测试网络

```bash
docker network create latency-test
```

### 3.2 启动两个测试容器

```bash
docker run -dit --name latency-c1 --network latency-test alpine sh
docker run -dit --name latency-c2 --network latency-test alpine sh
```

### 3.3 在发送端容器安装 Ping 工具

如果使用 alpine 容器，执行：

```bash
docker exec latency-c1 sh -c "apk add --no-cache iputils"
```

如果容器内已经有 `ping` 命令，可以跳过此步骤。

### 3.4 执行 Ping 测试

从发送端容器 `latency-c1` 向接收端容器 `latency-c2` 连续发送 500 个 ICMP 请求包：

```bash
docker exec latency-c1 ping -c 500 latency-c2
```

其中：

```text
-c 500
```

表示自动发送 500 次 Ping 请求，执行到 `icmp_seq=500` 后自动停止，并输出统计结果。

## 4. 结果读取方法

命令执行结束后，会输出类似结果：

```text
--- latency-c2 ping statistics ---
500 packets transmitted, 500 received, 0% packet loss
rtt min/avg/max/mdev = 0.026/0.035/0.120/0.006 ms
```

其中：

| 字段 | 含义 |
|---|---|
| packets transmitted | 发送的 Ping 包数量 |
| received | 成功接收响应的数量 |
| packet loss | 丢包率 |
| min | 最小往返时延 RTT |
| avg | 平均往返时延 RTT |
| max | 最大往返时延 RTT |
| mdev | 时延波动情况 |

本指标重点使用 `rtt min/avg/max/mdev` 中的 `min` 值。

## 5. 指标计算方法

假设测试结果为：

```text
rtt min/avg/max/mdev = 0.026/0.035/0.120/0.006 ms
```

则：

```text
最小往返时延 RTT_min = 0.026 ms
```

单向最小通信时延近似为：

```text
最小单向通信时延 ≈ 0.026 / 2 = 0.013 ms
```

换算为微秒：

```text
0.013 ms = 13 us
```

因此，该次测试得到：

容器间最小通信延迟约为 0.013 ms，约等于 13 us。
